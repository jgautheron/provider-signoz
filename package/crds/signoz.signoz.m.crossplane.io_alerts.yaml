---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: alerts.signoz.signoz.m.crossplane.io
spec:
  group: signoz.signoz.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - signoz
    kind: Alert
    listKind: AlertList
    plural: alerts
    singular: alert
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Alert is the Schema for the Alerts API. A SigNoz alert rule.
          Top-level fields are typed; the query condition, evaluation, and notification_settings
          are JSON blobs (export from the SigNoz UI to adapt). Defaults target the
          v5 / v2alpha1 schema required by SigNoz >= 0.125.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertSpec defines the desired state of Alert
            properties:
              forProvider:
                properties:
                  alert:
                    description: |-
                      (String) Alert name.
                      Alert name.
                    type: string
                  alertType:
                    description: |-
                      (String) One of METRIC_BASED_ALERT, LOGS_BASED_ALERT, TRACES_BASED_ALERT, EXCEPTIONS_BASED_ALERT.
                      One of `METRIC_BASED_ALERT`, `LOGS_BASED_ALERT`, `TRACES_BASED_ALERT`, `EXCEPTIONS_BASED_ALERT`.
                    type: string
                  condition:
                    description: |-
                      (String) Query condition as JSON (compositeQuery + thresholds). Compared semantically.
                      Query condition as JSON (compositeQuery + thresholds). Compared semantically.
                    type: string
                  description:
                    description: |-
                      readable description.
                      Human-readable description.
                    type: string
                  disabled:
                    description: |-
                      (Boolean) Whether the rule is disabled. Defaults to false.
                      Whether the rule is disabled. Defaults to false.
                    type: boolean
                  evalWindow:
                    description: |-
                      (String) Evaluation window (e.g. 5m0s).
                      Evaluation window (e.g. `5m0s`).
                    type: string
                  evaluation:
                    description: |-
                      (String) Evaluation block as JSON (v2alpha1+). Compared semantically.
                      Evaluation block as JSON (v2alpha1+). Compared semantically.
                    type: string
                  frequency:
                    description: |-
                      (String) Evaluation frequency (e.g. 1m0s).
                      Evaluation frequency (e.g. `1m0s`).
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Custom labels attached to the alert.
                      Custom labels attached to the alert.
                    type: object
                    x-kubernetes-map-type: granular
                  notificationSettings:
                    description: |-
                      (String) Notification settings as JSON (renotify, group_by, use_policy). Compared semantically.
                      Notification settings as JSON (renotify, group_by, use_policy). Compared semantically.
                    type: string
                  preferredChannels:
                    description: |-
                      (List of String) Notification channel names the alert routes to.
                      Notification channel names the alert routes to.
                    items:
                      type: string
                    type: array
                  ruleType:
                    description: |-
                      (String) Rule engine type: threshold_rule (default) or promql_rule.
                      Rule engine type: `threshold_rule` (default) or `promql_rule`.
                    type: string
                  schemaVersion:
                    description: |-
                      (String) Alert schema version. Defaults to v2alpha1.
                      Alert schema version. Defaults to `v2alpha1`.
                    type: string
                  severity:
                    description: |-
                      (String) Severity label (e.g. info, warning, critical).
                      Severity label (e.g. `info`, `warning`, `critical`).
                    type: string
                  source:
                    description: |-
                      (String) Source URL recorded on the rule.
                      Source URL recorded on the rule.
                    type: string
                  version:
                    description: |-
                      (String) Alert API version. Defaults to v5 .
                      Alert API version. Defaults to `v5` (required by SigNoz >= 0.125).
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alert:
                    description: |-
                      (String) Alert name.
                      Alert name.
                    type: string
                  alertType:
                    description: |-
                      (String) One of METRIC_BASED_ALERT, LOGS_BASED_ALERT, TRACES_BASED_ALERT, EXCEPTIONS_BASED_ALERT.
                      One of `METRIC_BASED_ALERT`, `LOGS_BASED_ALERT`, `TRACES_BASED_ALERT`, `EXCEPTIONS_BASED_ALERT`.
                    type: string
                  condition:
                    description: |-
                      (String) Query condition as JSON (compositeQuery + thresholds). Compared semantically.
                      Query condition as JSON (compositeQuery + thresholds). Compared semantically.
                    type: string
                  description:
                    description: |-
                      readable description.
                      Human-readable description.
                    type: string
                  disabled:
                    description: |-
                      (Boolean) Whether the rule is disabled. Defaults to false.
                      Whether the rule is disabled. Defaults to false.
                    type: boolean
                  evalWindow:
                    description: |-
                      (String) Evaluation window (e.g. 5m0s).
                      Evaluation window (e.g. `5m0s`).
                    type: string
                  evaluation:
                    description: |-
                      (String) Evaluation block as JSON (v2alpha1+). Compared semantically.
                      Evaluation block as JSON (v2alpha1+). Compared semantically.
                    type: string
                  frequency:
                    description: |-
                      (String) Evaluation frequency (e.g. 1m0s).
                      Evaluation frequency (e.g. `1m0s`).
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Custom labels attached to the alert.
                      Custom labels attached to the alert.
                    type: object
                    x-kubernetes-map-type: granular
                  notificationSettings:
                    description: |-
                      (String) Notification settings as JSON (renotify, group_by, use_policy). Compared semantically.
                      Notification settings as JSON (renotify, group_by, use_policy). Compared semantically.
                    type: string
                  preferredChannels:
                    description: |-
                      (List of String) Notification channel names the alert routes to.
                      Notification channel names the alert routes to.
                    items:
                      type: string
                    type: array
                  ruleType:
                    description: |-
                      (String) Rule engine type: threshold_rule (default) or promql_rule.
                      Rule engine type: `threshold_rule` (default) or `promql_rule`.
                    type: string
                  schemaVersion:
                    description: |-
                      (String) Alert schema version. Defaults to v2alpha1.
                      Alert schema version. Defaults to `v2alpha1`.
                    type: string
                  severity:
                    description: |-
                      (String) Severity label (e.g. info, warning, critical).
                      Severity label (e.g. `info`, `warning`, `critical`).
                    type: string
                  source:
                    description: |-
                      (String) Source URL recorded on the rule.
                      Source URL recorded on the rule.
                    type: string
                  version:
                    description: |-
                      (String) Alert API version. Defaults to v5 .
                      Alert API version. Defaults to `v5` (required by SigNoz >= 0.125).
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.alert is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.alert)
                || (has(self.initProvider) && has(self.initProvider.alert))'
            - message: spec.forProvider.alertType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.alertType)
                || (has(self.initProvider) && has(self.initProvider.alertType))'
            - message: spec.forProvider.condition is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.condition)
                || (has(self.initProvider) && has(self.initProvider.condition))'
          status:
            description: AlertStatus defines the observed state of Alert.
            properties:
              atProvider:
                properties:
                  alert:
                    description: |-
                      (String) Alert name.
                      Alert name.
                    type: string
                  alertType:
                    description: |-
                      (String) One of METRIC_BASED_ALERT, LOGS_BASED_ALERT, TRACES_BASED_ALERT, EXCEPTIONS_BASED_ALERT.
                      One of `METRIC_BASED_ALERT`, `LOGS_BASED_ALERT`, `TRACES_BASED_ALERT`, `EXCEPTIONS_BASED_ALERT`.
                    type: string
                  condition:
                    description: |-
                      (String) Query condition as JSON (compositeQuery + thresholds). Compared semantically.
                      Query condition as JSON (compositeQuery + thresholds). Compared semantically.
                    type: string
                  description:
                    description: |-
                      readable description.
                      Human-readable description.
                    type: string
                  disabled:
                    description: |-
                      (Boolean) Whether the rule is disabled. Defaults to false.
                      Whether the rule is disabled. Defaults to false.
                    type: boolean
                  evalWindow:
                    description: |-
                      (String) Evaluation window (e.g. 5m0s).
                      Evaluation window (e.g. `5m0s`).
                    type: string
                  evaluation:
                    description: |-
                      (String) Evaluation block as JSON (v2alpha1+). Compared semantically.
                      Evaluation block as JSON (v2alpha1+). Compared semantically.
                    type: string
                  frequency:
                    description: |-
                      (String) Evaluation frequency (e.g. 1m0s).
                      Evaluation frequency (e.g. `1m0s`).
                    type: string
                  id:
                    description: assigned alert rule id.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Custom labels attached to the alert.
                      Custom labels attached to the alert.
                    type: object
                    x-kubernetes-map-type: granular
                  notificationSettings:
                    description: |-
                      (String) Notification settings as JSON (renotify, group_by, use_policy). Compared semantically.
                      Notification settings as JSON (renotify, group_by, use_policy). Compared semantically.
                    type: string
                  preferredChannels:
                    description: |-
                      (List of String) Notification channel names the alert routes to.
                      Notification channel names the alert routes to.
                    items:
                      type: string
                    type: array
                  ruleType:
                    description: |-
                      (String) Rule engine type: threshold_rule (default) or promql_rule.
                      Rule engine type: `threshold_rule` (default) or `promql_rule`.
                    type: string
                  schemaVersion:
                    description: |-
                      (String) Alert schema version. Defaults to v2alpha1.
                      Alert schema version. Defaults to `v2alpha1`.
                    type: string
                  severity:
                    description: |-
                      (String) Severity label (e.g. info, warning, critical).
                      Severity label (e.g. `info`, `warning`, `critical`).
                    type: string
                  source:
                    description: |-
                      (String) Source URL recorded on the rule.
                      Source URL recorded on the rule.
                    type: string
                  version:
                    description: |-
                      (String) Alert API version. Defaults to v5 .
                      Alert API version. Defaults to `v5` (required by SigNoz >= 0.125).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
